# CI must pass before Railway deploys (enable Check Suites in Railway dashboard)
[build]
buildCommand = "pnpm run db:generate && pnpm run build:server"

[deploy]
startCommand = "pnpm run start"
preDeployCommand = "pnpm run db:migrate:deploy"
